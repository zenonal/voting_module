<%= form_for(@community, :html => { :multipart => true }) do |f| %>
  <% if @community.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @community.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

	<div class="field">
    <%= f.label :website %><br />
    <%= f.text_field :website_url %>
  </div>
  
  <div class="field">
    <%= f.file_field :uploadedPhoto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
